package com.alex.weatherapp.LoadingSystem.ForecastRequest;

/**
 * Created by Alex on 06.09.2015.
 */

/**
 * tag class, geollokup request has no such a class, because
 * geolookup data are obtained only fromn internet and are of WU type,
 * but forecast have two different formats - WU and inner format
 */
public class ForecastData {
}
